# Core RAG stack — llama-index-core only, not the `llama-index` meta-package
# (which drags in openai agent/program/question-gen subpackages we never import)
llama-index-core==0.14.21
llama-index-embeddings-huggingface==0.7.0
llama-index-llms-openai-like==0.7.2
llama-index-readers-file==0.6.0
llama-index-vector-stores-qdrant==0.10.0

# PyMuPDFReader is an optional extra of llama-index-readers-file — needs explicit pin
PyMuPDF==1.27.2.3

# Embedding runtime. torch is installed separately (CPU-only) in the Dockerfile.
sentence-transformers==5.4.1

# Vector store
qdrant-client==1.17.1

# App + utils
streamlit==1.57.0
python-dotenv==1.2.2
tqdm==4.67.3
